#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main(){
    int a,c;
    char b;
    scanf("%d%c%d",&a,&b,&c);
    switch(b)
        {
        case '+':b = '-';break;
        case '-':b = '+';break;
        case 'i':b = 'i';break;
        }
    if (b == )
        
        printf("%d%c%di",a,b,c);
    else
    return 0;
}