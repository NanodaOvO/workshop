#include <stdio.h>
#include <math.h>

int main()
{
    int n, m, t = 0, s = 0, i;

    scanf("%d%d", &m, &n);
    for (i = 0; i < n; i++)
    {
        t += k * pow(10, i);
        s += t;
    }
    printf("%d", s);
    return 0;
}
